{
  "name": "json-server",
  "version": "0.0.1",
  "description": "ARMAREMOS UN PEQUEÑO SERVIDOR",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"
  },
  "author": "Whillder",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}
